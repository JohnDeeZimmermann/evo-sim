# Evolution Simulator

This project is an Odin-based evolution simulator that models and renders creatures with ECS components and systems.

The local archetype ECS library is in `src/ecs`; see `docs/ecs/` for its query and deferred-change APIs before modifying simulation systems.
